"""Top-level package for the production ML system.

Note: This repository already had some legacy modules under `src/` used by the Flask app.
Those modules remain compatible. New production modules live under `src/data`, `src/features`,
`src/models`, `src/pipelines`, and `src/utils`.
"""

